#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    int sum = 0;
    int i = 1;  // start from 1

    while(i <= n) {
        if(i % 2 == 0) {  // check if even
            sum += i;
        }
        i++;
    }

    printf("%d", sum);
    return 0;
}
